"""Project-wide constants."""

# The WhatsApp version this release was tested against. Used by the TUI to
# warn (never block) when the connected device has a different version.
# Update this constant each release after verifying against a real device.
TESTED_WHATSAPP_VERSION = "2.26.13.73"
